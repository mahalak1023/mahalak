import 'package:flutter/material.dart';

/// Central color palette for Mahallak app
class AppColors {
  // Prevent instantiation
  AppColors._();

  /// Main brand color for primary buttons and main CTAs
  static const Color primaryBlue = Color(0xFF007BFF);

  /// Main text and headings
  static const Color darkGrey = Color(0xFF343A40);

  /// Background color
  static const Color lightGrey = Color(0xFFF8F9FA);

  /// Accent color for highlights, tags, small buttons
  static const Color accentTeal = Color(0xFF17A2B8);

  /// General white
  static const Color white = Color(0xFFFFFFFF);

  /// Error states and alerts
  static const Color errorRed = Color(0xFFDC3545);

  /// Success states and confirmations
  static const Color successGreen = Color(0xFF28A745);
}
